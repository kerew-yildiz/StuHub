# ── Uygulama verisi (asla commit edilmez) ─────────────────────────────
data/

# ── Kullanıcı materyalleri (demo/test dosyaları) ──────────────────────
Demo/

# ── Sırlar ────────────────────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Araçlar (workspace'te tutulan indirilmiş ikililer) ────────────────
.tools/

# ── Python ────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
.venv-win/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/
dist/
build/

# ── Node ──────────────────────────────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
.vite/

# ── Editör / OS ───────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# ── Ajan/araç artıkları (depo içinde birikiyor, kod değil) ────────────
# .claude/worktrees/ altında tam bir depo kopyası oluşabiliyor (2026-09-08'de
# 2.13 GB'lık kayıtlı bir worktree bulundu); .impeccable/ yerel kritik önbelleği.
.claude/
.impeccable/

# ── Geliştirici scratch dosyaları ─────────────────────────────────────
# Commit edilirse CI'daki `ruff check .` düşer (5 lint hatası taşıyor).
apps/backend/_stream_test3.py
apps/backend/_uvicorn_native.log
